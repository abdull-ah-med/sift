# Retrieve evals

PR-fast offline scoring of `evals/goldens/retrieve-pr-smoke.yaml` via
`uv run python evals/retrieve/run_offline.py`.

Live hybrid+TEI scoring and RAGAS/LongProbe LLM judges are Phase 4 / nightly.
