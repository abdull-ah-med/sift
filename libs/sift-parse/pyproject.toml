[project]
name = "sift-parse"
version = "0.0.0"
description = "Thin product adapter (`sift.parse`) over the vendored sift-parse engine."
requires-python = ">=3.13,<3.14"
license = { text = "Apache-2.0" }
dependencies = [
    "sift-core",
    "pydantic>=2.10",
    "pypdfium2>=5.12",
    "sift-parse-engine[convert-core,format-pdf,models-local,format-office,format-web,format-latex,format-email]",
    "sift-parse-core-engine",
    "sift-parse-pdf-engine",
    "sift-parse-models-engine",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sift"]

[tool.hatch.build.targets.wheel.sources]
"src/sift" = "sift"

[tool.uv.sources]
sift-core = { workspace = true }
sift-parse-engine = { workspace = true }
sift-parse-core-engine = { workspace = true }
sift-parse-pdf-engine = { workspace = true }
sift-parse-models-engine = { workspace = true }

[tool.pytest.ini_options]
testpaths = ["tests"]
