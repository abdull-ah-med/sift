[project]
name = "sift-worker"
version = "0.0.0"
description = "sift Taskiq workers (scaffold)."
requires-python = ">=3.13,<3.14"
license = { text = "Apache-2.0" }
dependencies = [
    "sift-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sift_worker"]

[tool.uv.sources]
sift-core = { workspace = true }
