[project]
name = "sift-api"
version = "0.0.0"
description = "sift HTTP API (FastAPI)."
requires-python = ">=3.13,<3.14"
license = { text = "Apache-2.0" }
dependencies = [
    "sift-core",
    "sift-parse",
    "sift-ingest",
    "sift-obs",
    "sift-retrieve",
    "sift-chat",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "python-multipart>=0.0.20",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.30",
    "psycopg[binary]>=3.2",
    "redis>=5.0",
    "httpx>=0.28",
    "pydantic-settings>=2.0",
    "PyJWT[crypto]>=2.10",
    "boto3>=1.35",
    "taskiq>=0.11",
    "taskiq-redis>=1.0",
    "langgraph>=1.2,<2",
    "langgraph-checkpoint-postgres>=2.0",
]


[project.optional-dependencies]
dev = [
    "httpx>=0.28",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sift_api"]

[tool.uv.sources]
sift-core = { workspace = true }
sift-parse = { workspace = true }
sift-ingest = { workspace = true }
sift-obs = { workspace = true }
sift-retrieve = { workspace = true }
sift-chat = { workspace = true }

[tool.pytest.ini_options]
testpaths = ["tests"]

